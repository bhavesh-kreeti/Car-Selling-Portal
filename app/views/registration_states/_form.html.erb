<%= form_with(model: registrationstate, local: true) do |form| %>
  <%= render 'shared/error', obj: @registrationstate %>
  <div class="field card-body">
    <%= form.label :name  %>
    <%= form.text_field :name, placeholder: 'Enter Registration State' %>
  </div>
  <div class="actions text-center">
    <%= form.submit class: 'btn btn-success' %>
  </div>
<% end %>
